# Minimal example project config
# Copy to /etc/go-host/projects.d/myapp.conf and edit values.

NAME=myapp
ENABLED=yes
DOMAIN=myapp.example.com
PORT=18080

# Optional overrides:
# USER=svc-myapp
# EXEC=/srv/apps/myapp/current/myapp

